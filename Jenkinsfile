pipeline {
    agent 'jenkins205'
    stages {
        stage ('build') {
            steps {
                sh 'php --version'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}